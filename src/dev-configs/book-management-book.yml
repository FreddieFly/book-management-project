
# H2 Database Configuration
spring:
  datasource:
    url: jdbc:h2:mem:book_db
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console